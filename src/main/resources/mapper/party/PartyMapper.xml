<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pbl2.sub119.backend.party.common.mapper.PartyMapper">

    <resultMap id="partyResultMap" type="pbl2.sub119.backend.party.common.entity.Party">
        <id property="id" column="id"/>
        <result property="productId" column="product_id"/>
        <result property="hostUserId" column="host_user_id"/>
        <result property="capacity" column="capacity"/>
        <result property="currentMemberCount" column="current_member_count"/>
        <result property="recruitStatus" column="recruit_status"/>
        <result property="operationStatus" column="operation_status"/>
        <result property="vacancyType" column="vacancy_type"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="terminatedAt" column="terminated_at"/>
        <result property="pricePerMemberSnapshot" column="price_per_member_snapshot"/>
    </resultMap>

    <!-- 새 멤버 등록 -->
    <insert id="insertParty" parameterType="pbl2.sub119.backend.party.common.entity.Party" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO party (
            product_id,
            host_user_id,
            capacity,
            current_member_count,
            recruit_status,
            operation_status,
            vacancy_type,
            created_at,
            updated_at,
            terminated_at,
            price_per_member_snapshot
        ) VALUES (
                     #{productId},
                     #{hostUserId},
                     #{capacity},
                     #{currentMemberCount},
                     #{recruitStatus},
                     #{operationStatus},
                     #{vacancyType},
                     #{createdAt},
                     #{updatedAt},
                     #{terminatedAt},
                     #{pricePerMemberSnapshot}
                 )
    </insert>

    <!-- 파티 단건 조회 -->
    <select id="findById" resultMap="partyResultMap">
        SELECT
            id,
            product_id,
            host_user_id,
            capacity,
            current_member_count,
            recruit_status,
            operation_status,
            vacancy_type,
            created_at,
            updated_at,
            terminated_at,
            price_per_member_snapshot
        FROM party
        WHERE id = #{partyId}
    </select>

    <!-- 참여/탈퇴/승계 처리 시 파티 상태 확인용 조회 -->
    <select id="findByIdForUpdate" resultMap="partyResultMap">
        SELECT
            id,
            product_id,
            host_user_id,
            capacity,
            current_member_count,
            recruit_status,
            operation_status,
            vacancy_type,
            created_at,
            updated_at,
            terminated_at,
            price_per_member_snapshot
        FROM party
        WHERE id = #{partyId}
            FOR UPDATE
    </select>

    <!-- 상품 기준 파티 목록 조회 -->
    <select id="findByProductId" resultMap="partyResultMap">
        SELECT
            id,
            product_id,
            host_user_id,
            capacity,
            current_member_count,
            recruit_status,
            operation_status,
            vacancy_type,
            created_at,
            updated_at,
            terminated_at,
            price_per_member_snapshot
        FROM party
        WHERE product_id = #{productId}
        ORDER BY created_at ASC
    </select>

    <!-- 현재 인원 수 업데이트 -->
    <update id="updateCurrentMemberCount">
        UPDATE party
        SET current_member_count = #{currentMemberCount},
            updated_at = NOW()
        WHERE id = #{partyId}
    </update>

    <!-- 모집 상태 변경 -->
    <update id="updateRecruitStatus">
        UPDATE party
        SET recruit_status = #{recruitStatus},
            updated_at = NOW()
        WHERE id = #{partyId}
    </update>

    <!-- 이용 상태 변경 -->
    <update id="updateOperationStatus">
        UPDATE party
        SET operation_status = #{operationStatus},
            updated_at = NOW()
        WHERE id = #{partyId}
    </update>

    <!-- 결원 상태 변경 -->
    <update id="updateVacancyType">
        UPDATE party
        SET vacancy_type = #{vacancyType},
            updated_at = NOW()
        WHERE id = #{partyId}
    </update>

    <!-- 승계 완료 시 파티장 변경 -->
    <update id="updateHostUserId">
        UPDATE party
        SET host_user_id = #{hostUserId},
            updated_at = NOW()
        WHERE id = #{partyId}
    </update>

    <!--참여 가능한 파티 목록 조회-->
    <select id="findJoinablePartiesByProductId" resultMap="partyResultMap">
        SELECT
            id,
            product_id,
            host_user_id,
            capacity,
            current_member_count,
            recruit_status,
            operation_status,
            vacancy_type,
            created_at,
            updated_at,
            terminated_at,
            price_per_member_snapshot
        FROM party
        WHERE product_id = #{productId}
          AND recruit_status = 'RECRUITING'
          AND operation_status IN ('WAITING_START', 'ACTIVE')
          AND current_member_count &lt; capacity
        ORDER BY (capacity - current_member_count) ASC, created_at ASC
    </select>

</mapper>