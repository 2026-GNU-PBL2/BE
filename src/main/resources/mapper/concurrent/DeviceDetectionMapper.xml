<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pbl2.sub119.backend.concurrent.mapper.DeviceDetectionMapper">

    <resultMap id="deviceDetectionResultMap" type="pbl2.sub119.backend.concurrent.entity.DeviceDetectionEvent">
        <id property="id" column="id"/>
        <result property="partyId" column="party_id"/>
        <result property="detectedDevice" column="detected_device"/>
        <result property="detectedLocation" column="detected_location"/>
        <result property="detectedAt" column="detected_at"/>
        <result property="status" column="status"
                javaType="pbl2.sub119.backend.concurrent.enumerated.DeviceDetectionStatus"/>
        <result property="notifiedUserIds" column="notified_user_ids"/>
        <result property="respondedUserIds" column="responded_user_ids"/>
        <result property="responseCount" column="response_count"/>
        <result property="mineCount" column="mine_count"/>
        <result property="unknownCount" column="unknown_count"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="pbl2.sub119.backend.concurrent.entity.DeviceDetectionEvent"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO device_detection_event (
            party_id, detected_device, detected_location,
            detected_at, status, notified_user_ids, expires_at, created_at, updated_at
        ) VALUES (
            #{partyId}, #{detectedDevice}, #{detectedLocation},
            #{detectedAt}, #{status}, #{notifiedUserIds}, #{expiresAt}, NOW(), NOW()
        )
    </insert>

    <select id="findById" resultMap="deviceDetectionResultMap">
        SELECT * FROM device_detection_event WHERE id = #{id}
    </select>

    <select id="findByPartyId" resultMap="deviceDetectionResultMap">
        SELECT * FROM device_detection_event WHERE party_id = #{partyId} ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE device_detection_event
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementMineCount">
        UPDATE device_detection_event
        SET mine_count = mine_count + 1, response_count = response_count + 1, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementUnknownCount">
        UPDATE device_detection_event
        SET unknown_count = unknown_count + 1, response_count = response_count + 1, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementResponseCount">
        UPDATE device_detection_event
        SET response_count = response_count + 1, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- FOR UPDATE로 행 락을 걸고 Java에서 중복 체크 후 갱신 -->
    <select id="findByIdForUpdate" resultMap="deviceDetectionResultMap">
        SELECT * FROM device_detection_event WHERE id = #{id} FOR UPDATE
    </select>

    <update id="updateRespondedUserIds">
        UPDATE device_detection_event
        SET responded_user_ids = #{respondedUserIds},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 상태 전환 1회 보장: PENDING일 때만 업데이트 -->
    <update id="updateStatusIfPending">
        UPDATE device_detection_event
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
          AND status = 'PENDING'
    </update>

    <select id="findExpiredPending" resultMap="deviceDetectionResultMap">
        SELECT * FROM device_detection_event
        WHERE status = 'PENDING' AND expires_at &lt; #{now}
    </select>

</mapper>
