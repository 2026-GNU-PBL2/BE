<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pbl2.sub119.backend.partyoperation.mapper.PartyOperationMapper">

    <resultMap id="partyOperationResultMap" type="pbl2.sub119.backend.partyoperation.entity.PartyOperation">
        <id property="id" column="id"/>
        <result property="partyId" column="party_id"/>
        <result property="operationType" column="operation_type"
                javaType="pbl2.sub119.backend.partyoperation.enumerated.OperationType"/>
        <result property="operationStatus" column="operation_status"
                javaType="pbl2.sub119.backend.partyoperation.enumerated.OperationStatus"/>
        <result property="inviteValue" column="invite_value"/>
        <result property="sharedAccountEmail" column="shared_account_email"/>
        <result property="sharedAccountPasswordEncrypted" column="shared_account_password_encrypted"/>
        <result property="operationGuide" column="operation_guide"/>
        <result property="operationStartedAt" column="operation_started_at"/>
        <result property="operationCompletedAt" column="operation_completed_at"/>
        <result property="lastResetAt" column="last_reset_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="partyOperationColumns">
        id,
        party_id,
        operation_type,
        operation_status,
        invite_value,
        shared_account_email,
        shared_account_password_encrypted,
        operation_guide,
        operation_started_at,
        operation_completed_at,
        last_reset_at,
        created_at,
        updated_at
    </sql>

    <!-- 파티 기준 현재 운영 정보 단건 조회 -->
    <select id="findByPartyId" resultMap="partyOperationResultMap">
        SELECT
        <include refid="partyOperationColumns"/>
        FROM party_operation
        WHERE party_id = #{partyId}
        LIMIT 1
    </select>

    <!-- 파티장이 파티 운영 정보 최초 생성 -->
    <insert id="insert"
            parameterType="pbl2.sub119.backend.partyoperation.entity.PartyOperation"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO party_operation
        (
            party_id,
            operation_type,
            operation_status,
            invite_value,
            shared_account_email,
            shared_account_password_encrypted,
            operation_guide,
            operation_started_at,
            operation_completed_at,
            last_reset_at,
            created_at,
            updated_at
        )
        VALUES
            (
                #{partyId},
                #{operationType},
                #{operationStatus},
                #{inviteValue},
                #{sharedAccountEmail},
                #{sharedAccountPasswordEncrypted},
                #{operationGuide},
                #{operationStartedAt},
                #{operationCompletedAt},
                #{lastResetAt},
                #{createdAt},
                #{updatedAt}
            )
    </insert>

    <!-- 파티장이 운영 정보 등록 및 수정 -->
    <update id="updateSetup">
        UPDATE party_operation
        SET
            operation_type = #{operationType},
            operation_status = #{operationStatus},
            invite_value = #{inviteValue},
            shared_account_email = #{sharedAccountEmail},
            shared_account_password_encrypted = #{sharedAccountPasswordEncrypted},
            operation_guide = #{operationGuide},
            operation_started_at = #{operationStartedAt},
            operation_completed_at = NULL,
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 멤버 진행 상황에 따라 운영 상태만 갱신 -->
    <update id="updateStatus">
        UPDATE party_operation
        SET
            operation_status = #{operationStatus},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 전원 완료 시 운영 상태 ACTIVE + 완료 시각 기록 -->
    <update id="updateStatusAndCompletedAt">
        UPDATE party_operation
        SET
            operation_status = #{operationStatus},
            operation_completed_at = #{operationCompletedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 재초대/비밀번호 변경 등으로 운영 재설정 처리 -->
    <update id="markResetRequired">
        UPDATE party_operation
        SET
            operation_status = #{operationStatus},
            last_reset_at = #{lastResetAt},
            operation_completed_at = NULL,
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

</mapper>