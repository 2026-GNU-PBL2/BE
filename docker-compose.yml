version: '3.8'

services:
  app:
    image: kawaiikoala/submate:latest
    container_name: springboot-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      TZ: Asia/Seoul
      SPRING_PROFILES_ACTIVE: prod
    restart: unless-stopped